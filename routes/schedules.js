const express = require('express');
const router = express.Router();

// Placeholder route for future scheduling functionality
router.get('/', (req, res) => {
  res.send('Schedules route');
});

module.exports = router;